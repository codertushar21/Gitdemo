[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "salary-tax"
version = "0.1.0"
description = "Simple salary tax calculator (CLI + optional GUI)"
requires-python = ">=3.10"

[project.scripts]
salary-tax = "salary_tax:main"
